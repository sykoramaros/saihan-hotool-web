{
  "name": "saihan-hotool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/frontend && bun run dev",
    "dev:cms": "cd apps/cms && bun run dev",
    "build": "cd apps/frontend && bun run build",
    "preview": "cd apps/frontend && bun run preview",
    "deploy": "ssh root@192.168.100.13 'cd /var/Docker/Saihan/saihan_hotool && ./deploy.sh'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
